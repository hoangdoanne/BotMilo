function run(cmd) {
  const arg3 = process.argv[3] || ''; // i.e. urlize

  const arg4 = process.argv[4] || ''; // i.e. someth!ng not URL friendly 😞

  switch (arg3) {
    case 'urlize':
    case 'urlise':
      console.log(urlize(arg4));
      break;

    default:
      console.log('invalid command');
      break;
  }
}

function urlize(input) {
  return input.replace(/\W/g, '-');
}

module.exports = {
  run
};